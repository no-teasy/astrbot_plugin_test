{
    "white_list_groups": {
        "description": "群聊白名单",
        "type": "list",
        "hint": "只有在白名单的群聊才启用点赞功能, 若没填写则默认全部群聊可用",
        "default": []
    },
    "subscribed_users": {
        "description": "订阅点赞的用户",
        "type": "list",
        "hint": "此变量用来存储订阅点赞用户的Q号,可不填",
        "default": []
    },
    "zanwo_date": {
        "description": "点赞日期",
        "type": "string",
        "hint": "如果检测到当前日期与点赞日期不同时，则触发自动点赞，并更新点赞日期",
        "invisible": true,
        "default": "2025-01-01"
    }
}